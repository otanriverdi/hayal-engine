#include "game.hpp"
#include "renderer.hpp"

void Game::Init(Memory &memory) {};

void Game::Update(const Input &input, const float dt,
                  Renderer::Commands &render_commands,
                  SoundBuffer &sound_buffer, Memory &memory) {};
